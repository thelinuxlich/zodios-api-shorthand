"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.formURLPlugin = void 0;
const zodios_error_1 = require("../zodios-error");
const plugin = {
    name: "form-url",
    request: (_, config) => __awaiter(void 0, void 0, void 0, function* () {
        if (typeof config.data !== "object" || Array.isArray(config.data)) {
            throw new zodios_error_1.ZodiosError("Zodios: application/x-www-form-urlencoded body must be an object", config);
        }
        return Object.assign(Object.assign({}, config), { data: new URLSearchParams(config.data).toString(), headers: Object.assign(Object.assign({}, config.headers), { "Content-Type": "application/x-www-form-urlencoded" }) });
    }),
};
/**
 * form-url plugin used internally by Zodios.
 * @example
 * ```typescript
 *   const apiClient = new Zodios(
 *     "https://mywebsite.com",
 *     [{
 *       method: "post",
 *       path: "/login",
 *       alias: "login",
 *       description: "Submit a form",
 *       requestFormat: "form-url",
 *       parameters:[
 *         {
 *           name: "body",
 *           type: "Body",
 *           schema: z.object({
 *             userName: z.string(),
 *             password: z.string(),
 *           }),
 *         }
 *       ],
 *       response: z.object({
 *         id: z.number(),
 *       }),
 *     }],
 *   );
 *   const id = await apiClient.login({ userName: "user", password: "password" });
 * ```
 * @returns form-url plugin
 */
function formURLPlugin() {
    return plugin;
}
exports.formURLPlugin = formURLPlugin;
