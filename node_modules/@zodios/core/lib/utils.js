"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.replacePathParams = exports.capitalize = exports.pick = exports.omit = void 0;
/**
 * omit properties from an object
 * @param obj - the object to omit properties from
 * @param keys - the keys to omit
 * @returns the object with the omitted properties
 */
function omit(obj, keys) {
    const ret = Object.assign({}, obj);
    for (const key of keys) {
        delete ret[key];
    }
    return ret;
}
exports.omit = omit;
/**
 * pick properties from an object
 * @param obj - the object to pick properties from
 * @param keys - the keys to pick
 * @returns the object with the picked properties
 */
function pick(obj, keys) {
    const ret = {};
    if (obj) {
        for (const key of keys) {
            ret[key] = obj[key];
        }
    }
    return ret;
}
exports.pick = pick;
/**
 * set first letter of a string to uppercase
 * @param str - the string to capitalize
 * @returns - the string with the first letter uppercased
 */
function capitalize(str) {
    return (str.charAt(0).toUpperCase() + str.slice(1));
}
exports.capitalize = capitalize;
const paramsRegExp = /:([a-zA-Z_][a-zA-Z0-9_]*)/g;
function replacePathParams(config) {
    let result = config.url;
    const params = config.params;
    if (params) {
        result = result.replace(paramsRegExp, (match, key) => key in params ? `${params[key]}` : match);
    }
    return result;
}
exports.replacePathParams = replacePathParams;
